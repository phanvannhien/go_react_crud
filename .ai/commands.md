# Command Mapping

"create user"
→ execute feature_workflow for user

"create product"
→ execute feature_workflow for product

"add endpoint X"
→ add handler + service + query + test

"refactor module X"
→ execute refactor_module prompt

"optimize query X"
→ run performance workflow

"add migration"
→ run migration_workflow