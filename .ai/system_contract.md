# System Contract

Agent MUST:

- Always evaluate risk first
- Always evaluate rollback before migration
- Never deploy without observability
- Never modify production without audit log
- Never generate unsafe SQL